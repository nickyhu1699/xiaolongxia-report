# 2026-03-08 周六

## 配置工作

### 网页抓取：Jina.ai Reader
- 创建 `jina_reader.py` 脚本
- 更新 `TOOLS.md` 添加工具说明
- 更新 `AGENTS.md` 添加工具选择指南

**优势：**
- 能绕过付费墙
- 能抓取推特
- 返回干净的 Markdown
- 免费，不需要 API key

**用法：**
```bash
python3 ~/workspace/jina_reader.py <URL>
```

### 浏览器自动化：Playwright
- 创建 `browser_fetch.py` 脚本
- 支持无头和有头模式
- 能处理 JavaScript 渲染的网页
- 支持截图和 PDF 导出

**用法：**
```bash
# 无头模式
python3 ~/workspace/browser_fetch.py <URL>

# 有头模式
python3 ~/workspace/browser_fetch.py --headed <URL>

# 截图
python3 ~/workspace/browser_fetch.py --screenshot <URL>
```

**测试结果：**
- ✅ Jina.ai Reader: 普通网站完美，付费墙部分有效
- ✅ 浏览器自动化: 无头模式测试通过

## 其他工作

### AI 新闻周报
- 搜索并发送了最新 AI 新闻到邮箱
- 重点：GPT-5.4、中国 AI 战略、WordPress AI 插件

### 蕾特恩祛痘负面信息
- 搜索整理了蕾特恩的负面信息
- 发送到邮箱
- 黑猫投诉 5597+ 条，涉及美容贷、强制推销、退款难等问题
